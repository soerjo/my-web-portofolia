services:
  nextjs-app:
    build: 
      context: .
    restart: unless-stopped
    expose:
      - "3000"
    env_file:
      - .env
    networks:
      - internal_net

networks:
  internal_net:
    external: true