services:
  nextjs-app:
    build: 
      context: .
    restart: unless-stopped
    expose:
      - "3000"
    ports:
      - "3000:3000"
    networks:
      - internal_net

networks:
  internal_net:
    driver: bridge